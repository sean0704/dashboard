# 專案需求文件 (PRD)：個人化家庭資產儀表板

## 1. 專案目標
建立一個基於 GitHub Pages 的私有化家庭資產追蹤工具。透過「網頁輸入、AES 加密儲存、GitHub 託管」的流程，實現免資料庫、高隱私（端對端加密）且完全自主掌控的財務儀表板。

## 2. 核心功能需求
### A. 資產數據輸入與安全儲存 (Data Management & Security)
**成員管理 (四位成員)：**
- 固定支援 4 位家庭成員（成員1、成員2、成員3、成員4）的資產與負債數據輸入。

**動態輸入表單與部位明細管理：**
- **月份選擇**：預設當月（格式為 `YYYY-MM`）。
- **資產分類輸入**：
  - 現金：直接輸入數值。
  - 國內股票、複委託、海外券商：支援成員獨立之明細編輯彈窗（可輸入多筆標的名稱、槓桿倍數與價值），系統以帳面價值自動加總帶回主輸入表單（維持會計平衡），並於風險分析時乘上槓桿倍數換算實質曝險。
  - 房產：直接輸入數值。
- **負債分類輸入**：
  - 房貸：拆分為「一般型房貸」、「增貸型房貸 (一)」、「增貸型房貸 (二)」、「理財型房貸」，直接輸入數值。
  - 信用貸款：直接輸入數值。
  - 股票質押：直接輸入數值。

**安全加密與資料導入：** 
- **設置密碼**：生成密文 JSON 前需提示使用者設置或輸入「家庭金庫密碼」。
- **AES 加密**：使用 AES-256 演算法將完整的歷史資產數據（包含所有成員的明細項目）轉換為密文字串，供使用者下載覆蓋 `database.json`。
- **智慧帶入與歷史修改**：切換至已有紀錄之歷史月份時，系統自動帶入該月份當初儲存之所有資產、負債與各成員標的明細（含槓桿倍數），並顯示編輯覆蓋提示；提供歷史月份快速載入標籤與「還原此月舊紀錄」功能；亦支援一鍵帶入最新一期紀錄與清空表單，大幅提升修改舊數據與新增新月份之體驗。

### B. 安全解鎖與視覺化分析 (Decryption & Visualization)
**安全解鎖機制：**
- 網頁載入時若偵測到 `database.json` 為加密密文，強制顯示「解鎖螢幕」。
- 密碼僅在瀏覽器本地端進行 AES 解密，絕不傳送至任何伺服器。

**互動式多維度篩選：**
- **成員篩選**：提供核取方塊（Checkbox），可動態選擇顯示特定成員的資產。
- **類別篩選**：提供資產與負債各分類項目的核取方塊，可動態排除或包含特定類別。

**視覺化圖表與數據表 (AI Optimized & Analytical)：**
- **資產總覽數據卡**：呈現總資產、總負債、淨資產，以及「股票槓桿倍數」（以槓桿倍數為主數值，支援槓桿型 ETF 倍數計算實質名目曝險；卡片底部直接列出動態除法算式，並附有 ⓘ 氣泡說明視窗詳細解說計算原理與機制）。
- **資產配置圓餅圖**：使用 Chart.js 繪製，呈現各資產類別的佔比。
- **個股與標的配置明細表**：自動合併不同成員持有的相同標的名稱，呈現標的槓桿標籤、帳面市值與實質曝險金額，並按實質曝險金額降序排列。未歸類明細的資產會自動以「未分類 [類別名]」呈現。
- **淨資產增長折線圖**：繪製歷史淨資產（總資產減總負債）的增長趨勢。
- **每月數據分析表**：以表格形式呈現歷史數據，並自動計算：
  - 月增減金額
  - 月增率（MoM Growth Rate）
  - 年化複合成長率（CAGR，以歷史第一筆數據為基底計算至該月）

**數據概覽（AI 分析友善）：**
- 提供「一鍵複製」功能，將當月的資產、各類別標的市值明細、負債、淨資產彙整為純粹的會計資產負債結構化純文字（排除衍生槓桿與曝險指標），便於複製至外部 AI 工具進行深度財務諮詢。

### C. 頁面導航 (Page Navigation)
- **頁面切換**：主頁面預設顯示「資產儀表板」（包含圖表、明細與摘要）。使用者可透過頂部導航列切換至「資產數據輸入」頁面。

## 3. 技術規格與架構
**部署環境**：GitHub Pages (靜態代碼)。

**開發技術**：
- **UI 框架**：Tailwind CSS (透過 CDN `@2.2.19`)。
- **圖表引擎**：Chart.js (透過 CDN `@3.7.0`)。
- **加密庫**：CryptoJS (AES-256，透過 CDN `@4.1.1`)。
- **資料邏輯**：Vanilla JavaScript。

**資料流與結構**：
- **資產資料結構 (Schema)**：
  - 歷史數據為一個陣列，依月份排序。
  - 一般項目：`number`。
  - 明細項目：`{ total: number, details: [{ name: string, value: number, leverage?: number }] }`。
  - 槓桿倍數：預設為 `1`（正2填 `2`，反一填 `-1`），計算實質名目曝險時使用。
- **資料儲存**：`database.json` 儲存 AES 密文字串。
- **合併邏輯**：產出個股明細與數據概覽時，需將不同成員持有的相同標的（IgnoreCase 或 Trim）合併加總。

## 4. 介面設計建議 (RWD)
- **桌面端**：採用雙欄式排版，左側為數據卡片、配置圖與個股明細表；右側為寬幅折線圖與每月數據分析表。
- **手機端**：改為單欄式排列。
- **細節設計**：輸入表單及彈窗應支援 RWD 響應式佈局，明細彈窗設有最大高度並支援內部滾動。
