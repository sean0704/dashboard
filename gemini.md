# 專案需求文件 (PRD)：個人化家庭資產儀表板

## 1. 專案目標
建立一個基於 GitHub Pages 的私有化家庭資產追蹤工具。透過「網頁輸入、AES 加密儲存、GitHub 託管」的流程，實現免資料庫、高隱私（端對端加密）且完全自主掌控的財務儀表板。

## 2. 核心功能需求
### A. 資產數據輸入與安全儲存 (Data Management & Security)
**動態輸入表單：**
- 月份選擇：預設當月。
- **部位明細管理 (New)：** 針對「國內股票、複委託、海外券商」提供明細編輯彈窗。支援輸入個別標的名稱與價值，系統自動加總回主表單。
- 資產分類輸入：現金、國內股票、複委託、海外券商、房產。
- 負債輸入：房貸、卡債。

**安全加密：** 
- **設置密碼：** 生成 JSON 前需設置「家庭金庫密碼」。
- **AES 加密：** 使用 AES-256 演算法將資產數據轉換為加密字串。
- **智慧帶入：** 支援帶入最新一期紀錄，包含所有標的明細。

### B. 安全解鎖與視覺化分析 (Decryption & Visualization)
**金庫解鎖：** 網頁載入時偵測加密數據，強制顯示「解鎖螢幕」，需輸入正確密碼透過瀏覽器本地解密方可顯示數據。
**完整數據概覽 (AI Optimized)：** 提供「一鍵複製」功能，輸出包含所有資產部位明細的完整文字格式，便於外部 AI 進行財務分析。
**家庭成員篩選：** 提供核取方塊（Checkbox）讓使用者能選擇顯示特定成員的資產數據。
**資產配置與趨勢圖：** Chart.js 繪製配置圓餅圖與淨資產增長折線圖（含 CAGR 計算）。

### C. 頁面導航 (Page Navigation)
**頁面切換：** 主頁面預設顯示「資產儀表板」。使用者可透過導航切換至「資產數據輸入」頁面。

## 3. 技術規格與架構
**部署環境：** GitHub Pages (靜態代碼)。

**開發技術：**
- **UI 框架：** Tailwind CSS (透過 CDN)。
- **圖表引擎：** Chart.js。
- **加密庫：** CryptoJS (AES-256)。
- **資料邏輯：** Vanilla JavaScript。

**資料流設計：**
- **資產資料結構**：
  - 一般項目：`number`。
  - 明細項目：`{ total: number, details: [{ name: string, value: number }] }`。
- **加密與解密**：`database.json` 儲存 AES 密文字串，由瀏覽器端完成解碼。
- **合併邏輯**：數據概覽輸出時會自動合併不同成員持有的相同標的，呈現家庭總曝險。

## 4. 介面設計建議 (RWD)
- **桌面端：** 左側為數據卡片與圓餅圖，右側為寬幅的折線趨勢圖。
- **手機端：** 改為單欄式排列。
